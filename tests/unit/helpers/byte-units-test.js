import {
  byteUnits
} from 'gitcharts/helpers/byte-units';

module('ByteUnitsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = byteUnits(42);
  ok(result);
});
